学习笔记


## Search algo

### BFS
- implemented with `queue`
- `queue = push() + shift()` or
- `queue = pop() + unshift()`
- `stack = push() + pop()`

### BFS A*
- first search points that are closer to the end point
